{
  "inputs": [
    {
      "type": "promptString",
      "id": "mcp-api-key",
      "description": "ISUCON MCP Server API Key (run: azd env get-value MCP_API_KEY)",
      "password": true
    },
    {
      "type": "promptString",
      "id": "mcp-url",
      "description": "ISUCON MCP Server URL (run: azd env get-value MCP_SERVER_FQDN)"
    }
  ],
  "servers": {
    "isucon13-mcp": {
      "type": "http",
      "url": "https://${input:mcp-url}/mcp",
      "headers": {
        "Authorization": "Bearer ${input:mcp-api-key}"
      }
    }
  }
}
