name: sre-agent-isucon13
metadata:
  template: sre-agent-isucon13

infra:
  provider: bicep
  path: infra

services:
  isucon-mcp-server:
    host: containerapp
    project: ./isucon-mcp-server
    image: isucon-mcp-server
    docker:
      path: ./Dockerfile

hooks:
  postprovision:
    shell: sh
    run: scripts/post-provision.sh
